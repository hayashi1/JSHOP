    <% items.each do |item| %>
      <% item_image = image_tag(item['smallImageUrls'][0]['imageUrl'], size:'64x64', alt:item['itemName']) %>
      <dd><%= link_to(item_image, item['itemUrl'], target: '_blank') %></dd>
    <% end %>

